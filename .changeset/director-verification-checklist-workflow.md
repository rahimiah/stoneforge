---
"@stoneforge/smithy": patch
---

Update the built-in director workflow prompt to require a `Verification Checklist` section for tasks that can be independently verified by a steward, including a standard checklist item format with explicit `testType` values.
